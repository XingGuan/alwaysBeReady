### 响应式设计 (Responsive Design)
+ 媒体查询(`Media Queries`)：`@media (max-width:768px){ …… }`，根据视口大小应用不同样式。  
+ 相对单位：`%`,`vw/vh`(视口宽/高),`rem`(相对于根元素字体大小)，`em`（相对于父元素或自身大小）。  
+ 为什么重要：让网站在手机、平板、桌面等不同尺寸的设备上都能完美显示是现代`Web`开发的标配。   



