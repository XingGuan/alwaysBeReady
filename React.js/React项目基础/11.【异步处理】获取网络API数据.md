### 异步处理、动态显示 获取网络API数据  

+ `AJAX`  
+ `callback`  无法多线程的天生依赖，都是用 callback 函数来处理异步逻辑。
+ 程序栈`stack`、先进后出。 回调函数的本质就是建立程序栈，利用先进后出的原则通过把回调函数已参数的形式放入回调栈中，然后按照一定的顺序依次执行。  
  
#### `Callback Hell`
#### `Promise`
[mdn Promise](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Promise)


### 提供API假数据网站  
[jsonplaceholder](https://jsonplaceholder.typicode.com/)
